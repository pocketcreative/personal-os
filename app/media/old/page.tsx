import ContentBoard from '@/components/content/ContentBoard';

// The old Media Tracker (content_pieces table), archived from /media on
// 2026-09-26. Nothing about it changed, only its route.
export default function OldMediaPage() {
  return <ContentBoard />;
}
